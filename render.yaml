services:
  - type: web
    name: ai-resume-analyzer
    env: python
    branch: main
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

# NOTE: Render will use this file to auto-deploy the service. Set the
# required environment variables in the Render dashboard (see README).
